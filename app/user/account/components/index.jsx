'use strict'

import React from 'react';
import axios from 'axios';

export default ({ user }) => {
  console.log('USER: ', user);
  return (
    <div>
      <h2>User Account Page</h2>
    </div>
  );
};
